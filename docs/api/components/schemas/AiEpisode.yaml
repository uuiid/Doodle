type: object
properties:
  id:
    type: string
    format: uuid
  name:
    type: string
    description: 剧集名称
  description:
    type: string
    description: 剧集描述
  subproject_id:
    type: string
    format: uuid
    description: 所属子项目 ID
  created_at:
    type: string
    format: date-time
  entity_id:
    type: string
    format: uuid
    description: 对应的 AI 生成实体
  limit_count:
    type: integer
    format: int64
    description: 生成次数限制，默认 0

delete:
  tags:
    - 数据-项目
  summary: 删除状态自动化
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_project_settings_status_automations_instance::delete`，实现见 `project.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_project_settings_status_automations_instance_delete
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: status_automation_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

get:
  tags:
    - 数据-用户
  summary: 获取待检查任务
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `tasks_to_check::get`，实现见 `task.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: tasks_to_check_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []

get:
  tags:
    - 数据-用户
  summary: 获取已完成任务
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_user_done_tasks::get`，实现见 `task.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_user_done_tasks_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

get:
  tags:
    - 认证
  summary: 登出
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `auth_logout::get`，实现见 `auth.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: auth_logout_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []

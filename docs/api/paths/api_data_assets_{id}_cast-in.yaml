get:
  tags:
    - 数据-资产
  summary: 获取cast-in
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_assets_cast_in::get`，实现见 `assets.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_assets_cast_in_get
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: array
            items: {}
  security:
    - bearerAuth: []

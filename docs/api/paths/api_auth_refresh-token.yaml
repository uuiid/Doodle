get:
  tags:
    - 认证
  summary: 刷新令牌
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `auth_refresh_token::get`，实现见 `login.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: auth_refresh_token_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []

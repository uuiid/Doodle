post:
  tags:
    - 动作-人员
  summary: 修改密码
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_persons_change_password::post`，实现见 `user.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_persons_change_password_post
  parameters:
    - name: person_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '500':
      description: 两次输入密码不一致
  security:
    - bearerAuth: []

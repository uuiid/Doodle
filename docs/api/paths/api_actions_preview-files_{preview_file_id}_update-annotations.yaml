put:
  tags:
    - 动作-预览文件
  summary: 更新标注
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_preview_files_update_annotations::put`，实现见 `playlists.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_preview_files_update_annotations_put
  parameters:
    - name: preview_file_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

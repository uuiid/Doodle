post:
  tags:
    - 动作-用户
  summary: 全部标记已读
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_user_notifications_mark_all_as_read::post`，实现见 `notifications.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_user_notifications_mark_all_as_read_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

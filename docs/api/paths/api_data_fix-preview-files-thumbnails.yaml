post:
  tags:
    - 数据
  summary: 修复预览缩略图
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_fix_preview_files_thumbnails::post`，实现见 `preview.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_fix_preview_files_thumbnails_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '204':
      description: 成功，无响应体
  security:
    - bearerAuth: []

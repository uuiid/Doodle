post:
  tags:
    - 动作-预览文件
  summary: 合成视频
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_preview_files_compose_video::post`，实现见 `compose_video.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_preview_files_compose_video_post
  parameters:
    - name: preview_file_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '400':
      description: 必须上传视频文件；没有找到相关的预览文件；无法打开视频文件 {} {}；视频帧数不匹配 {} != {}
  security:
    - bearerAuth: []

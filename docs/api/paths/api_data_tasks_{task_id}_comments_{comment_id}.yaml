delete:
  tags:
    - 数据-任务
  summary: 删除评论
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `task_comment::delete`，实现见 `comment.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: task_comment_delete
  parameters:
    - name: task_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: comment_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

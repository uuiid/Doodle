get:
  tags:
    - 影片
  summary: 获取预览文件瓦片
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `movies_tiles_preview_files::get`，实现见 `movies.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: movies_tiles_preview_files_get
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: 文件内容
      content:
        image/png:
          schema:
            type: string
            format: binary
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

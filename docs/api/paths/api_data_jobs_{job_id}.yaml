get:
  tags:
    - 任务队列
  summary: 获取任务队列详情
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_jobs_instance::get`，实现见 `jobs.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_jobs_instance_get
  parameters:
    - name: job_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []
put:
  tags:
    - 任务队列
  summary: 更新任务队列
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_jobs_instance::put`，实现见 `jobs.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_jobs_instance_put
  parameters:
    - name: job_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []
delete:
  tags:
    - 任务队列
  summary: 删除任务队列
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_jobs_instance::delete`，实现见 `jobs.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_jobs_instance_delete
  parameters:
    - name: job_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '204':
      description: 成功，无响应体
    '405':
      description: 任务正在运行中, 无法删除
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

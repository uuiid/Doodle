put:
  tags:
    - 数据-工作室
  summary: 更新工作室
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `studios_instance::put`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: studios_instance_put
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '500':
      description: 工作室名称不可为空
  security:
    - bearerAuth: []
delete:
  tags:
    - 数据-工作室
  summary: 删除工作室
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `studios_instance::delete`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: studios_instance_delete
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '204':
      description: 成功，无响应体
  security:
    - bearerAuth: []

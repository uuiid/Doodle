get:
  tags:
    - 数据-计算机
  summary: 获取计算机详情
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_computers_instance::get`，实现见 `computers.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_computers_instance_get
  parameters:
    - name: computer_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []
put:
  tags:
    - 数据-计算机
  summary: 更新计算机
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_computers_instance::put`，实现见 `computers.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_computers_instance_put
  parameters:
    - name: computer_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []
delete:
  tags:
    - 数据-计算机
  summary: 删除计算机
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_computers_instance::delete`，实现见 `computers.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_computers_instance_delete
  parameters:
    - name: computer_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

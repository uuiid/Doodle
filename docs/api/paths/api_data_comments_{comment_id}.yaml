put:
  tags:
    - 数据-评论
  summary: 更新评论
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_comment::put`，实现见 `comment.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_comment_put
  parameters:
    - name: comment_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []
get:
  tags:
    - 数据-评论
  summary: 获取评论详情
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_comment::get`，实现见 `comment.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_comment_get
  parameters:
    - name: comment_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []

get:
  tags:
    - 数据-输出类型
  summary: 获取输出类型列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_output_types::get`，实现见 `output_types.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_output_types_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: array
            items: {}
  security:
    - bearerAuth: []

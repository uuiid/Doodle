post:
  tags:
    - 动作-项目
  summary: 搜索
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_projects_search::post`，实现见 `assets.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_projects_search_post
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

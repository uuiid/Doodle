get:
  tags:
    - 图片
  summary: 获取组织缩略图
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `pictures_thumbnails_organisations_png::get`，实现见 `pictures.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: pictures_thumbnails_organisations_png_get
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: 文件内容
      content:
        image/png:
          schema:
            type: string
            format: binary
    '500':
      description: 组织缩略图不存在 组织 id {}
  security:
    - bearerAuth: []

post:
  tags:
    - Doodle
  summary: 执行备份
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `doodle_backup::post`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: doodle_backup_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []

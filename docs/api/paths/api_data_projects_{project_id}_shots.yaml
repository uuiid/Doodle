get:
  tags:
    - 数据-项目
  summary: 获取镜头列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_project_shots::get`，实现见 `shots.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_project_shots_get
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '204':
      description: 成功，无响应体
  security:
    - bearerAuth: []
post:
  tags:
    - 数据-项目
  summary: 创建镜头
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_project_shots::post`，实现见 `shots.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_project_shots_post
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '400':
      description: '未知的序列 id '
  security:
    - bearerAuth: []

get:
  tags:
    - 数据-项目
  summary: 获取选角
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_project_sequences_all_casting::get`，实现见 `casting.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_project_sequences_all_casting_get
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

post:
  tags:
    - 动作-任务
  summary: 创建审阅
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_tasks_create_review::post`，实现见 `compose_video.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_tasks_create_review_post
  parameters:
    - name: task_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '400':
      description: 集数名称必须包含换行符
    '500':
      description: 无法找到附件文件 {}
  security:
    - bearerAuth: []

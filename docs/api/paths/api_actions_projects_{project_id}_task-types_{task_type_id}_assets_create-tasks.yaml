post:
  tags:
    - 动作-项目
  summary: 创建任务
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_create_tasks::post`，实现见 `project.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_create_tasks_post
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: task_type_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []

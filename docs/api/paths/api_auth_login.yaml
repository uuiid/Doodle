post:
  tags:
    - 认证
  summary: 登录
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `auth_login::post`，实现见 `login.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: auth_login_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
    '400':
      description: email 或 password 为空；密码错误超过5次,请1分钟后再试；密码错误
    '401':
      description: 用户未激活

get:
  tags:
    - 数据-资产
  summary: 获取资产详情
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `asset_details::get`，实现见 `assets.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: asset_details_get
  parameters:
    - name: asset_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []
delete:
  tags:
    - 数据-资产
  summary: 删除资产
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `asset_details::delete`，实现见 `assets.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: asset_details_delete
  parameters:
    - name: asset_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

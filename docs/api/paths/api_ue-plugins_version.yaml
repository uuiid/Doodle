get:
  tags:
    - UE插件
  summary: 获取版本
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `ue_plugins_version::get`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: ue_plugins_version_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []
post:
  tags:
    - UE插件
  summary: 获取版本
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `ue_plugins_version::post`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: ue_plugins_version_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

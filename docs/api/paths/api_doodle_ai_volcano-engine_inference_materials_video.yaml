post:
  tags:
    - Doodle
  summary: 创建视频
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `doodle_ai_volcano_engine_inference_materials_video::post`，实现见 `pictures.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: doodle_ai_volcano_engine_inference_materials_video_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '500':
      description: 请上传视频文件

get:
  tags:
    - 数据-更新日志
  summary: 获取更新日志列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_updata_logs::get`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_updata_logs_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []
post:
  tags:
    - 数据-更新日志
  summary: 创建更新日志
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_updata_logs::post`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_updata_logs_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

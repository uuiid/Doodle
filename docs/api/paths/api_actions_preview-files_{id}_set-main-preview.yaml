put:
  tags:
    - 动作-预览文件
  summary: 设为主预览
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_preview_files_set_main_preview::put`，实现见 `preview.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_preview_files_set_main_preview_put
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '400':
      description: mp4文件不支持设置为主预览文件
  security:
    - bearerAuth: []

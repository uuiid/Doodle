post:
  tags:
    - 认证
  summary: 重置密码
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `auth_reset_password::post`，实现见 `auth.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: auth_reset_password_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
put:
  tags:
    - 认证
  summary: 重置密码
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `auth_reset_password::put`，实现见 `auth.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: auth_reset_password_put
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '400':
      description: 无效的重置令牌。；Passwords do not match.

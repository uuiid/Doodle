post:
  tags:
    - 数据-项目
  summary: 创建序列
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_project_sequences::post`，实现见 `sequences.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_project_sequences_post
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []
get:
  tags:
    - 数据-项目
  summary: 获取序列列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_project_sequences::get`，实现见 `sequences.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_project_sequences_get
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

get:
  tags:
    - 数据-实体
  summary: 获取动态
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_entities_news::get`，实现见 `entities.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_entities_news_get
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

get:
  tags:
    - 动作-项目
  summary: 获取工作文件
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_projects_sequences_working_file::get`，实现见 `working_file.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_projects_sequences_working_file_get
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    - name: sequence_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '404':
      description: '未知的序列 id ；未知的项目 id '
  security:
    - bearerAuth: []

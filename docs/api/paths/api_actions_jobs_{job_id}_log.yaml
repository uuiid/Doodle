get:
  tags:
    - 任务队列
  summary: 获取日志
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_jobs_log::get`，实现见 `jobs.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_jobs_log_get
  parameters:
    - name: job_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '204':
      description: 成功，无响应体
    '401':
      description: 权限不足
  security:
    - bearerAuth: []
put:
  tags:
    - 任务队列
  summary: 获取日志
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `actions_jobs_log::put`，实现见 `jobs.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: actions_jobs_log_put
  parameters:
    - name: job_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '204':
      description: 成功，无响应体
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

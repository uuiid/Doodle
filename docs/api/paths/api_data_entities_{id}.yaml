get:
  tags:
    - 数据-实体
  summary: 获取实体详情
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_entities::get`，实现见 `entities.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_entities_get
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []
put:
  tags:
    - 数据-实体
  summary: 更新实体
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_entities::put`，实现见 `entities.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_entities_put
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []

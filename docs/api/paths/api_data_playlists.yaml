post:
  tags:
    - 数据-播放列表
  summary: 创建播放列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_playlists::post`，实现见 `playlists.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_playlists_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

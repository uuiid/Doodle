get:
  tags:
    - 数据-项目
  summary: 获取授权列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_project_authorization::get`，实现见 `auth_outsource.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_project_authorization_get
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []
post:
  tags:
    - 数据-项目
  summary: 创建授权
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_project_authorization::post`，实现见 `auth_outsource.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_project_authorization_post
  parameters:
    - name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

get:
  tags:
    - 数据-工作室
  summary: 获取工作室列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `studios::get`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: studios_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []
post:
  tags:
    - 数据-工作室
  summary: 创建工作室
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `studios::post`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: studios_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
    '500':
      description: 工作室名称不可为空
  security:
    - bearerAuth: []

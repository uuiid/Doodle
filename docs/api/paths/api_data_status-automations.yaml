get:
  tags:
    - 数据-状态自动化
  summary: 获取状态自动化列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `status_automations::get`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: status_automations_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
  security:
    - bearerAuth: []
post:
  tags:
    - 数据-状态自动化
  summary: 创建状态自动化
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `status_automations::post`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: status_automations_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

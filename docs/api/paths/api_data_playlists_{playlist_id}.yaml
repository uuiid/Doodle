put:
  tags:
    - 数据-播放列表
  summary: 更新播放列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_playlists_instance::put`，实现见 `playlists.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_playlists_instance_put
  parameters:
    - name: playlist_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
    '401':
      description: 权限不足
  security:
    - bearerAuth: []
delete:
  tags:
    - 数据-播放列表
  summary: 删除播放列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_playlists_instance::delete`，实现见 `playlists.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_playlists_instance_delete
  parameters:
    - name: playlist_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '204':
      description: 成功，无响应体
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

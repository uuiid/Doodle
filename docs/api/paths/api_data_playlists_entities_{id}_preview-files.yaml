get:
  tags:
    - 数据-播放列表
  summary: 获取预览文件列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `playlists_entities_preview_files::get`，实现见 `playlists.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: playlists_entities_preview_files_get
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema: {}
    '401':
      description: 权限不足
  security:
    - bearerAuth: []

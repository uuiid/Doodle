put:
  tags:
    - 数据-实体类型
  summary: 更新实体类型
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_entity_types_instance::put`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_entity_types_instance_put
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []
delete:
  tags:
    - 数据-实体类型
  summary: 删除实体类型
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_entity_types_instance::delete`，实现见 `base_data.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_entity_types_instance_delete
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '204':
      description: 成功，无响应体
  security:
    - bearerAuth: []

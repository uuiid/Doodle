post:
  tags:
    - 数据-人员
  summary: 创建人员
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_person::post`，实现见 `user.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_person_post
  requestBody:
    required: true
    content:
      application/json:
        schema: {}
  responses:
    '201':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []
get:
  tags:
    - 数据-人员
  summary: 获取人员列表
  description: |
    机器生成：注册于 `kitsu.cpp`，处理器为 `data_person::get`，实现见 `user.cpp`。

    请求体与响应体的字段结构尚未逐字段整理，请以处理器实现为准。
  operationId: data_person_get
  responses:
    '200':
      description: JSON 响应（字段结构尚未逐字段整理）
      content:
        application/json:
          schema:
            type: object
  security:
    - bearerAuth: []

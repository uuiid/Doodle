# ����Googletest 
find_package(GTest CONFIG REQUIRED)

# include ͷ�ļ�

include_directories(${GTest_DIR})
include_directories(${GTEST_INCLUDE_DIRS}
        ${GMOCK_INCLUDE_DIRS})

include_directories(${PROJECT_SOURCE_DIR})


#get_cmake_property(_variableNames VARIABLES)
#foreach (_variableName ${_variableNames})
#    message(STATUS "${_variableName}=${${_variableName}}")
#endforeach ()

add_executable(test_my_class
        "test_fileInfo.cpp"
        "test_sqlComm.cpp"
        "main.cpp")

target_link_libraries(test_my_class
        PRIVATE GTest::gtest GTest::gtest_main GTest::gmock GTest::gmock_main filesyn)
add_test(AllTestsInMain ${CMAKE_CURRENT_BINARY_DIR}/debug/test_my_class)
#gtest_discover_tests(test_my_class)
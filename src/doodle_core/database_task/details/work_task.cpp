#include "work_task.h"

namespace doodle::database_n {

void sql_com<doodle::work_task_info>::create_table(conn_ptr& in_ptr) {
  
}
void sql_com<doodle::work_task_info>::install(conn_ptr& in_ptr, const entt::handle& in_handle) {
  
}
void sql_com<doodle::work_task_info>::update(conn_ptr& in_ptr, const entt::handle& in_handle) {
  
}
void sql_com<doodle::work_task_info>::select(conn_ptr& in_ptr, entt::handle& in_handle) {
  
}
void sql_com<doodle::work_task_info>::destroy(conn_ptr& in_ptr, const entt::handle& in_handle) {
  
}

}  // namespace doodle::database_n